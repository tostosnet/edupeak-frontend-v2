import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
    title: 'Education at its Peak',
};

export default function HomePage() {
    return <HomeView />;
}
